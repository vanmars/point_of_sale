<%= render "layouts/errors", :object => @product %>

<%= form_for [@purchase, @product] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :price %>
  <%= f.text_field :price %>

  <%= f.submit %>
<% end %>